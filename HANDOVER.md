# 引き継ぎ書

Web版 Claude Code（2026年9月）から、デスクトップ版へ引き継ぐためのメモ。
**このファイルと `README.md` を読めば、経緯を知らなくても作業を続けられる**ようにしてある。

---

## 1. これは何か

愛知県瀬戸市のシニア世帯向け家事代行サービス「陽だまり家事サポート」という設定の、
**架空のポートフォリオ用サイト**。実在の事業者ではない。会社名・住所・お客様の声・
保険の記載はすべて架空。

- 公開URL: https://taisun3son-create.github.io/test-claude-1/
- ブランチ: `claude/housekeeping-service-webpage-avt6qy`（`main` ではない）
- 9ページ（うち `thanks.html` は noindex）
- 検証 235項目、いま全部通っている

## 2. デスクトップ版で最初にやること

```bash
git clone https://github.com/taisun3son-create/test-claude-1.git
cd test-claude-1
git checkout claude/housekeeping-service-webpage-avt6qy

npm install                      # playwright（検証用）
npx playwright install chromium  # ブラウザ本体
```

必要なのは **Python 3** と **Node.js** だけ。サイト自体に外部ライブラリの依存はない。

```bash
npm run build   # python3 build.py と同じ。HTMLを生成する
npm test        # サーバを立てて235項目を検証し、終わったら片づける
```

`npm test` は先にビルドしてから測るので、ソースと生成物がずれた状態で
「通った」と誤解することはない。1つだけ流すときは `node tests/run.js tel` のように書く。

## 3. 仕組み（ここだけ押さえれば触れる）

```
build.py          ← ヘッダー・フッター・<head>・電話番号・住所はここ1か所
src/page-*.html   ← 各ページの本文だけ
assets/           ← style.css / main.js / 画像
tests/            ← 検証。run.js が全部まとめて流す
*.html            ← 生成物。直接編集しても次のビルドで消える
```

**`index.html` などを直接編集しないこと。** `src/` か `build.py` を編集して
`npm run build` を実行する。9ページに同じヘッダーをコピーすると、1か所直すのに
9回同じ作業が要るのでこうしてある。

ビルド時に**リンク切れを検査**して、飛び先のないリンクがあると止まる。
1ページを6ページに分けたとき、`#reserve` のようなページ内リンクが8か所
どこにも飛ばなくなった事故があったため。

## 4. 決まっていること

設計上の判断とその理由は **`README.md`** に全部書いてある。特に効いているのは:

- **推測で直さない。** px・画面数・コントラスト比・ミリ秒で測ってから直す
- **テストは値の一致ではなく、意味で書く。** 「文字色が背景色と違う」ではなく
  「コントラスト比が4.5:1以上」。前者だと同じ種類のバグを取り逃す
  （実際に「ご予約」の見出しが背景と同色になって消えた）
- **JavaScript が動かなくても壊れない。** 料金カードの開閉ボタンはJSで作る。
  HTMLに書くと、JSが動かない環境で閉じたまま開けなくなる
- **タップ領域44px・文字13px以上。** 利用者に高齢者を想定している
- **実在しうる連絡先を置かない。** 電話番号は未割当の局番（`0561-00-0000`）、
  住所は市まで。それらしい値を置くと、実在する誰かに問い合わせが行く

公開手順とフォームの設定は **`DEPLOY.md`**。

## 5. 残っている宿題

| 件 | 状況 |
|---|---|
| **検索エンジンに登録するか** | **未決。持ち主の判断待ち。** いまは全ページ登録可。架空の事業者が「瀬戸市 家事代行」で出てくる状態 |
| おためしプランの料金 | **矛盾したまま。** 2時間5,500円＝時間あたり2,750円で、定期・週1回と同単価。「おためし」なのに安くない |
| フォームの送信先 | 未設定（`src/page-reserve.html` の `data-endpoint=""`）。押しても完了画面が出るだけで、どこにも届かない |
| 写真 | 表紙の1枚のみ。本来は作業風景や代表の顔写真が要る |
| アクセス解析 | 未導入。Cloudflare Web Analytics 推奨（Cookieを使わないので同意バナー不要） |

`24523121_l.jpg` は表紙写真の元データ。`assets/cover-*.{jpg,webp,avif}` の生成元。

## 6. これからの案件の進め方

**1案件 = 1リポジトリ = 1フォルダ**にする。デスクトップ版 Claude Code は
フォルダ単位で開くので、案件ごとに分けておくと、前の案件の文脈が混ざらない。

```
~/web/
  hidamari-seto/       ← この案件
  次の案件/
  _template/           ← 使い回す土台
```

このサイトの `build.py` / `tests/` / `README.md` の書き方は、そのまま次の案件の
土台に使える。`_template/` に切り出しておくと、2件目以降が速くなる。
中身（社名・色・文言）だけ差し替えれば形になる。

> **確認したいこと**：「それぞれのページごとで作りたい」を、
> 上のように「案件ごとにフォルダを分ける」と解釈した。
> もし「1つの案件の中でページごとにファイルを分けたい」という意味なら、
> それは `src/page-*.html` として既にそうなっている。

## 7. 新しいセッションの Claude に伝えること

最初にこう言えば早い。

> このリポジトリの `HANDOVER.md` と `README.md` を読んで。
> 変更は `src/` か `build.py` を編集して `npm run build`、
> 確認は `npm test`。ブランチは `claude/housekeeping-service-webpage-avt6qy`。

**この会話のやり取り自体は引き継がれない。** 決まっていないこと（上の「残っている宿題」）は
改めて伝える必要がある。
